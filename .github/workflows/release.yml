name: Create release from tag

on:
  push:
    tags:
      - "v*"

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@a70053709ad859e4611b228778656fa2f2712658
      - name: Create release
        run: gh release create --repo ${{ github.repository }} ${{ github.ref_name }} original.txt
        env:
          GH_TOKEN: ${{ github.token }}
